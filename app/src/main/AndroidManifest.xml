<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.picnicstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PicnicStore">
        <activity
            android:name=".MainActivity"
            android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pickupActivity" android:label="Pickup activity" android:launchMode="singleTop"/>
        <activity android:name=".deliveryActivity" android:label="Delivery activity" android:launchMode="singleTop"/>
        <activity android:name=".StoreActivity" android:label="Store activity" android:launchMode="singleTop"/>
        <activity android:name=".RatingActivity" android:label="rating" android:launchMode="singleTop"/>




    </application>

</manifest>